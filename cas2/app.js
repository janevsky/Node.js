const { filterByGoalsScored, filterByName } = require('./filters')
const players = require('./data')
const os = require('os')

console.log(players)

// console.log(filterByGoalsScored(players, 16))
// console.log(filterByName(players, 'Boban'))
// console.log(os.version())

/**
 * Ima 3 vidovi na moduli:
 * 1. File-based modules
 * 2. Core modules
 * 3. External modules
 */

 /**
  * 2 files za evidencija:
  * 1. package.json
  * 2. package-lock.json
  */

